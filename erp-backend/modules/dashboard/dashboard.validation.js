/**
 * Dashboard takes no request body — the single role-aware endpoint derives its
 * payload entirely from the authenticated user (§5.17). No schema needed; this
 * file exists to keep the 4-file module shape consistent.
 */
export {};
